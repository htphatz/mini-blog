server:
  port: 8082
  servlet:
    context-path: /notification

spring:
  data:
    mongodb:
      authentication-database: admin
      username: root
      password: root
      database: notification-service
      port: 27017
      host: localhost
